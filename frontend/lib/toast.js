export const toast = {
    success: (message) => {
        // Simple alert for now, can be replaced with a toast library
        alert(message);
    },
    error: (message) => {
        alert(message);
    },
    info: (message) => {
        alert(message);
    },
};

