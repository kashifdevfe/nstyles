# Railway Configuration
# IMPORTANT: In Railway dashboard, set Root Directory to 'backend' for backend service
# Using Nixpacks (no Docker needed)

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run prisma:generate && npx prisma migrate deploy && npm start"

